// Adapter contract — populated in Phase 2 when the process boundary makes
// the interface concrete (ADR-0005).
//
// Phase 1 sources are in-core and do not use this module. It exists now so
// downstream crates can import `fm_adapter_sdk::contract` without breakage
// when the trait is introduced.
